plugins {
    id "com.github.node-gradle.node" version "7.0.0"
}

tasks.register( "npm-run-build", NpmTask) {
    onlyIf {
        gradle.taskGraph.hasTask(':shadowJar')
    }

    args = ['run', 'build']
}